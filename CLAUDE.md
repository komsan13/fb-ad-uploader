# CLAUDE.md — fb-ad-uploader

รวมกับ `C:/Users/User/CLAUDE.md` (ตัว global) — ไฟล์นี้เป็นกฎเฉพาะของโปรเจกต์นี้

---

## กฎข้อที่ 1: ห้ามเดา ต้องหาข้อมูลก่อนเสมอ

**ก่อนสรุปสาเหตุของอะไรก็ตาม ต้องมีหลักฐานที่วัดได้ ไม่ใช่เหตุผลที่ฟังดูสมเหตุสมผล**

ระบบนี้ใช้เงินโฆษณาจริงอัตโนมัติ การเดาผิดหนึ่งครั้งแปลว่าเงินหายจริง หรือบัญชีโดนแบนจริง

### ทำแบบนี้

1. **หาเอกสารทางการก่อน** — Meta Marketing API, Anthropic API ใช้ `claude-api` skill
   อย่าตอบจากความจำเรื่อง API ของคนอื่น
2. **วัดของจริง** ไม่ใช่อ่านโค้ดแล้วสรุป
   - หน้าเว็บ/พิกเซล/JS → `gstack browse` โหลดหน้าจริงแล้วดู network + console
   - เส้นทาง FB API → เรียก API จริงด้วย token จริง (อ่านอย่างเดียว ปลอดภัย)
   - สถานะบนเซิร์ฟเวอร์ → ssh เข้าไปดู ไม่ใช่เดาจากโค้ด
3. **แยกให้ออกระหว่าง "แก้ปัญหาได้" กับ "เจอสาเหตุแล้ว"**
   บางทีแก้ถูกโดยบังเอิญแต่สาเหตุเป็นอีกเรื่อง ถ้ายังไม่พิสูจน์ ต้องบอกว่ายังไม่รู้
4. **บอกตรงๆ เมื่อยังพิสูจน์ไม่ได้** ดีกว่าเสนอทฤษฎีที่ฟังดูดี

### เคสจริงที่พลาดมาแล้ว (19 ก.ค. 2026)

| เดาว่า | ความจริง |
|---|---|
| Meta หาพิกเซลไม่เจอเพราะ `/lp` อยู่หลัง basic auth | basic auth เป็นปัญหาจริงและร้ายแรง (คนกดแอดเจอหน้าใส่รหัส) **แต่ไม่ใช่สาเหตุของ error นั้น** |
| ใช้ `last_fired_time` ของพิกเซลยืนยันว่า event เข้าไหม | ค่านี้ **cache และอัปเดตไม่สม่ำเสมอ** ให้ผลขัดแย้งกันเอง เสียเวลาไล่เช็คสองรอบ |

ตัวที่ให้คำตอบจริงคือ **Events Manager → Test Events** และการโหลดหน้าด้วยเบราว์เซอร์จริงแล้วดู network

ระหว่างไล่หาสาเหตุด้วยการวัดจริง ถึงเจอบั๊กที่มีอยู่จริง: `fbq('track', ...)` แบบไม่ระบุพิกเซล
ยิงเข้าทุกพิกเซลที่ init ไว้พร้อมกัน มี 10 พิกเซล + 10 บล็อก = PageView ถูกนับเกินจริงหลายเท่า
ถ้าไม่เจอ ตัวขยายงบกับตัวปิดแอดขาดทุนจะตัดสินใจจากตัวเลขที่เฟ้อ

---

## กฎข้อที่ 2: แตะของ production ต้องระวัง

- **ห้ามใส่คอมเมนต์กลางคำสั่งที่ต่อบรรทัดด้วย `\`** (เช่น `docker run`) คำสั่งจะขาดกลาง
  เคยทำเว็บล่มและรหัสผ่าน basic auth หายมาแล้ว เพราะ `redeploy.sh` ดึง hash จาก container เดิม
- รัน `bash -n <script>` ก่อนเอาสคริปต์ไปรันบนเซิร์ฟเวอร์จริงเสมอ
- ทดสอบด้วย `CONFIG_PATH` ชี้ไป scratchpad **อย่ายิงใส่ไฟล์ config จริง**
- ก่อนแก้ config บนเซิร์ฟเวอร์ ให้สำรองก่อน (`cp config.json config.json.bak-<เหตุผล>`)

---

## กฎข้อที่ 2.5: เซิร์ฟเวอร์ห้ามถือ session ของเบราว์เซอร์ผู้ใช้

**token/cookie ที่เกิดจากการล็อกอินในเบราว์เซอร์ ห้ามส่งออกมาให้เซิร์ฟเวอร์ใช้ ไม่ว่ากรณีใด**

ใช้ได้เฉพาะ token ที่ได้จาก OAuth ของแอปเราเอง ซึ่ง Meta ออกแบบมาให้เรียกจากเซิร์ฟเวอร์อยู่แล้ว

### เคสจริงที่พลาดมาแล้ว (26 ก.ค. 2026)

ทำฟีเจอร์อ่าน "วงเงินใช้จ่ายต่อวัน" (`adtrust_dsl`) ซึ่งอ่านได้เฉพาะ token ของ Ads Manager
เขียนส่วนขยาย Chrome ดึง token จากเบราว์เซอร์ผู้ใช้ ส่งมาเก็บที่เซิร์ฟเวอร์ แล้วให้เซิร์ฟเวอร์ยิง Graph

ผู้ใช้กดใช้**ครั้งเดียว** ผลที่ได้เรียงกันดังนี้:

| ลำดับ | สิ่งที่เกิด |
|---|---|
| 1 | เซิร์ฟเวอร์ยิง Graph ด้วย token นั้น → `(#1) Invalid request` |
| 2 | ครั้งถัดมา → `(#190) the session has been invalidated ... for security reason` |
| 3 | บัญชี FB ของผู้ใช้โดน checkpoint ทั้งบัญชี (`190/459`) ต้องยืนยันตัวตนทางอีเมล |
| 4 | token ของ autopilot ใช้ไม่ได้ไปด้วย = ระบบล่มทั้งระบบ |

เหตุผล: session token ของผู้ใช้คนหนึ่งโผล่ไปใช้จากคนละเครื่อง คนละ IP ภายในไม่กี่นาที
คือลายเซ็นของการขโมยบัญชีในสายตา Meta — มันจึงล็อกบัญชีทันที

**ทางที่ถูก:** ให้ส่วนขยายยิง API เองในเบราว์เซอร์ผู้ใช้ (IP/session เดียวกับที่ล็อกอิน)
แล้วส่งเข้าเซิร์ฟเวอร์แค่ "ผลลัพธ์" ดูของจริงที่ `extension/background.js` + `/api/am-limits`

**เกราะที่ห้ามถอด:** `loadConfig()` ใน `server.js` ล้าง `amToken` ทิ้งทุกครั้งที่โหลด และไม่มีเส้น API
ที่รับ token ของ Ads Manager อีกแล้ว มีเทสคุมไว้ที่ `test/autopilot.test.js`
("เซิร์ฟเวอร์ต้องเก็บ Ads Manager token ไม่ได้อีกเลย") กับ `test/extension.test.js`
("token ต้องไม่ถูกส่งออกไปไหนเลย")

---

## กฎข้อที่ 3: เทสก่อนบอกว่าเสร็จ

`npm test` — 46 เทส ต้องผ่านหมดก่อน commit

- `test/fake-fb.js` — Facebook Graph API ปลอม เสียบผ่าน env `FB_API_BASE`
- `test/autopilot.test.js` — รัน server.js จริงคุยกับ FB ปลอม ยืนยันจาก state + request ที่ FB ได้รับ
- `test/landing.test.js` — หน้า Landing, การอัปโหลด, ความปลอดภัยของลิงก์
- `test/logic.test.js` — ตรรกะล้วน เรียกฟังก์ชันจริงผ่าน `module.exports`

เทสชั่วคราวที่เขียนขึ้นมารันแล้วทิ้ง **ไม่นับ** — เคยรายงานว่า "ทดสอบแล้วผ่านหมด"
ทั้งที่มี P0 สองตัวซ่อนอยู่ เพราะเทสเฉพาะสิ่งที่ตัวเองคิดว่าจะพัง

งานที่แก้เยอะหรือแตะเรื่องเงิน ให้ส่ง subagent ไปรีวิวแบบ adversarial ด้วย
รอบที่แล้วเจอ P0 สองตัวที่ตัวเองมองไม่เห็น

---

## บริบทของระบบที่ต้องรู้

- **Deploy:** `git push` แล้ว ssh เข้า `root@31.97.105.21` รัน `bash redeploy.sh`
  ข้อมูลจริงอยู่ที่ `/opt/fbad-data/` (config, autopilot-state, คลังสื่อ, landing)
- **หน้า `/lp` ต้องเปิดสาธารณะเสมอ** ส่วน `/lp/admin` และ `/api/*` ต้องอยู่หลัง basic auth
  (router `fbadpub` ใน `redeploy.sh`)
- **เงิน:** `minAds` × จำนวนบัญชี × `campaignBudget` คืองบรายวันจริง
  10 บัญชี × 3,333 = 33,330 บาท/วัน ทวนเลขนี้ก่อนแนะนำให้เปิดระบบเสมอ
- **โหมดทดสอบ** (`autopilot.testMode`) สร้างแอดจริงแต่ปิดไว้ ใช้เงิน 0 บาท
  ใช้ตัวนี้ก่อนเสมอเมื่อจะลองอะไรใหม่
- **เกราะกันภัยที่ห้ามถอด (4 ตัว มี `rail: true` ใน `AP_LIMIT_SPEC`):** แอดหนึ่งตัวขึ้นใหม่ได้
  กี่ครั้ง (`maxRelaunchPerAd` ตั้งต้น 1 = แก้ข้อความครั้งเดียวแล้วจบ), แก้ข้อความได้วันละกี่ครั้ง,
  โดนปฏิเสธ 3 ตัวใน 24 ชม. หยุดทั้งบัญชี, หมวดเหตุผลเดิมซ้ำ 2 ครั้งหยุดเติมแอด
  เทส `test/logic.test.js` บังคับว่า `max ≤ def × 2.5` ของทุกตัว — **ห้ามขยายกรอบนี้**
- **การสับคลิปหลังโดนปฏิเสธ** (29 ก.ค. 2026) เปิดต่อเมื่อตั้ง `maxRelaunchPerAd` เกิน 1 เท่านั้น
  ทำงานเฉพาะตอน AI ตัดสินว่าปัญหาอยู่ที่ **ตัววิดีโอ** (`where === 'video'`) — สับไปคลิปที่ยังไม่
  พิสูจน์ว่าแย่ แล้วขึ้นใหม่ใน adset เดิม ข้อความไม่แตะ
  ผู้ใช้สั่งทำเองหลังได้รับคำเตือนแล้วว่านี่คือการยิงซ้ำหลังโดนปฏิเสธ ซึ่ง Meta ใช้จับ ban evasion
  **ห้ามถอดเงื่อนไขกันเหล่านี้:** ติด `noRotate` (หมวดเดิมซ้ำ) ห้ามขึ้นใหม่, นับรวมใน `maxFixPerDay`,
  ครบเพดานต้องตายถาวร, state รุ่นเก่าที่มีแต่ `retryOf` ต้องนับเป็นขึ้นใหม่แล้ว 1 ครั้ง
